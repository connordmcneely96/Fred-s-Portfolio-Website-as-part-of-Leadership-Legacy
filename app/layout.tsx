import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Graphic Designer Portfolio | Creative Design Services",
  description: "Professional graphic designer specializing in branding, logo design, social media content, print materials, and digital illustrations. Let's bring your vision to life.",
  keywords: "graphic design, logo design, branding, social media design, print design, illustration",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="font-sans">
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
